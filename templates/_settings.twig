{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: "Target Field"|t,
    instructions: "Set the target field of this relation."|t,
    id: 'targetField',
    name: 'targetField',
    options: fields,
    value: settings.targetField
})}}

{{ forms.lightswitchField({
    label: "Read-only"|t,
    instructions: "Disallow adding or removing of relations."|t,
    id: 'readOnly',
    name: 'readOnly',
    on: settings.readOnly
})}}
